{% for block in entry.blocks.level(1).all %}
    {% set background = block.background
        ? block.background.color|first
        : { color: '#FFF', class: 'bg-white' } %}

    {% include 'modules/blocks/_' ~ block.type.handle ignore missing %}
{% endfor %}
